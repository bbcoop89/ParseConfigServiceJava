#password
password: p@ssw0rd

server:
    applicationConnectors:
        - type: http
          port: 8080
        - type: https
          port: 8443
          keyStorePath: parseconfig.keystore
          keyStorePassword: p@ssw0rd
          validateCerts: false

database:
    driverClass: com.mysql.jdbc.Driver
    user: root
    password: root
    url: jdbc:mysql://localhost:8889/parse_config_j
